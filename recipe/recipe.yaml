schema_version: 1

context:
  name: jmespath
  version: 1.1.0
  bundle: tar.gz
  hash_val: 472c87d80f36026ae83c6ddd0f1d05d4e510134ed462851fd5f754c8c3cbb88d

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name }}-${{ version }}.${{ bundle }}
  sha256: ${{ hash_val }}
  file_name: ${{ name }}-${{ version }}.${{ bundle }}

build:
  number: 1
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}

tests:
  - python:
      imports:
        - jmespath
      python_version: ${{ python_min }}.*
about:
  license: MIT
  license_file: LICENSE
  summary: JSON Matching Expressions
  homepage: https://github.com/jmespath/jmespath.py
  repository: https://github.com/jmespath/jmespath.py

extra:
  recipe-maintainers:
    - pmlandwehr
    - xylar
